# memory 运行记忆区

这个目录只用于你本地保存真实问卦案例、反馈和复盘结果。默认公开版本应保持空白。

- `casebook.jsonl`：已完成案例库。默认不参与新卦判断。
- `feedback_log.jsonl`：事后反馈记录。默认不参与新卦判断。
- `incoming_cases/`：临时导入案例。不要上传 GitHub。

核心原则：新卦默认 fresh 模式，只用当前卦盘、六爻规则和古籍检索；旧案例只能在用户明确要求“参考类案/复盘/学习”时使用。
